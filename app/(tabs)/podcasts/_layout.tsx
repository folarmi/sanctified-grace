import { Stack } from "expo-router";

const PodcastLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    ></Stack>
  );
};

export default PodcastLayout;
